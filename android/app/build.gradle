plugins {
    id "com.android.application"
    id "kotlin-android"
    id "com.google.gms.google-services"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.sorimachi.flutterProject" // 🔹 Đổi thành namespace của bạn
    compileSdk 35

    defaultConfig {
        // 🔹 Application ID (phải trùng với AndroidManifest.xml)
        applicationId "com.sorimachi.flutterProject"
        minSdkVersion flutter.minSdkVersion
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            // 🔹 Dùng keystore nếu bạn có, còn không dùng debug key mặc định
            signingConfig signingConfigs.debug
            minifyEnabled false
            shrinkResources false
        }
    }

    // 🔹 Tùy chọn: thêm compileOptions cho Java 17/21 nếu cần
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true // 👈 Thêm dòng này
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

flutter {
    source "../.."
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.9.24"
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.4' // 👈 Thêm dòng này
}
